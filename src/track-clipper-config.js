/** Clipper offset tuning — standalone so Node tests avoid importing full Constants (car patterns need browser THREE). */

export const clipperScale = 1000;
export const clipperCenterlineMultiplier = 2;
export const clipperMaxCenterlinePoints = 10000;
export const clipperArcToleranceFactor = 0.02;
