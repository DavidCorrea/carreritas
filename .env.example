DATABASE_URL=postgres://user:password@host/database?sslmode=require
JWT_SECRET=some-random-secret-at-least-32-chars
# Set to "true" to use the pg driver for local Postgres (omit in production with Neon)
USE_LOCAL_DB=
